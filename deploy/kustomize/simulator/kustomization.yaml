apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service-zenoh.yaml

configMapGenerator:
  - name: simulator-zenoh-config
    files:
      - config.json5=zenoh-config.json5
    options:
      disableNameSuffixHash: true
